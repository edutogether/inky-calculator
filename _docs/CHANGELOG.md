# CHANGELOG

InKY Calculator의 변경 이력. **git 커밋 기록에서 확인되는 사실만** 적는다 — 확인 안 되는 것은
적지 않는다(DOC-STANDARD 원칙). 날짜는 커밋 메시지의 승인 날짜 기준.

## 2026-09-10 (계속)

- **CI 배포 판정을 별도 `changes` 잡으로 뺐다**(§23-3 정정). 처음엔 deploy 잡 안에서
  판정했는데, 팀장 지시로 다른 저장소와 같은 구조(판정 전용 잡 + `if: needs.changes...`)로
  맞췄다. "바뀐 파일 0개" 같은 애매한 경우를 전부 배포 쪽으로 기울이는 fail-open도
  이때 보강했다.
- **첫 종합감사(10/10)를 마쳤다.** 새로 발견한 결함 4건을 모두 고쳤다 — 엑셀
  "가져오기"가 조작된 수량·지급액을 검증하지 않던 것(어제 고친 `#q=` 취약점과 같은
  종류가 다른 경로에 남아 있었음), GitHub Actions 워크플로의 GITHUB_TOKEN 과다 권한,
  `check-headers.js`에 빠져 있던 HSTS 검사, vitest의 중간등급 취약점(5.0.0으로 업그레이드,
  `npm audit` 0건). 프리즈 태그 `inky-calculator-freeze-20260910-audited-100`을 찍고
  `git checkout`으로 실제 롤백까지 리허설했다.
- **CI 워크플로를 test/deploy 두 잡으로 나눴다.** 문서(`.md`·`_docs/`·`.claude/`)만 바뀐
  커밋은 배포·헤더 확인을 건너뛴다(`COMMON_STANDARDS` §23) — `app.md`가 저장소에서 가장
  길어서 문서 한 줄만 고쳐도 매번 배포가 돌던 낭비를 줄였다. 검사(test) 잡은 문서 변경과
  무관하게 항상 돈다.
- **app.md "알려진 것" 목록에 남아 있던 항목을 전부 처리했다.**
  - `#mCap`·`#mPer`의 자릿점 표기를 맞췄다 — `#mPer`는 render()가 매번 콤마를 다시
    그렸는데 `#mCap`은 그러지 않아 첫 화면에서 `40000`·`20,000`으로 서로 다르게 보이던
    것을 고쳤다.
  - 캡션(`.cap` — `#mNote`·`#optN`이 공유) 대비를 `--dim`(라이트 3.08:1)에서
    `--mut`(라이트 5.49:1·다크 6.55:1)로 올려 WCAG AA를 지키게 했다.
    `scripts/check-contrast.js`를 새로 만들어 WCAG 상대 휘도 공식으로 실제 계산해 검사한다.
  - 부스 이름의 `AI ` 접두어 불일치(①②의 데이터 이름과 탭 이름이 서로 달랐던 것)를
    스크린샷으로 대표님께 확인받아 **"둘 다 AI 없이"**로 통일했다.
  - 파비콘 `/favicon.ico` 404는 고치려면 "배포는 index.html 한 개만 나간다"는 이
    저장소의 최종 구조를 깨야 한다는 것을 대표님께 설명하고, **"그냥 둔다"**로 확정했다.
  - `frame-ancestors 'none'`은 애초에 버그가 아니라 의도한 보안 설계였다고 재분류했다.
  - `tests/scenarios.test.js`에 시나리오 9(자릿점 표기)·10(AI 접두어)을 추가했다 —
    전부 코드를 되돌려 실패를 확인한 뒤 넣었다(§5.4/§21-2).
- **외부 스크립트에 SRI(`integrity`)를 걸었다.** `xlsx.full.min.js`(cdnjs, 버전 고정 불변
  파일)에 cdnjs 공식 SRI 해시와 `crossorigin="anonymous"`를 추가했다. 구글 폰트 CSS는
  브라우저 User-Agent에 따라 다른 바이트를 돌려주는 리소스라 SRI를 걸 수 없다는 것을 확인하고
  그대로 두었다. 헤드리스 Chrome(CDP)으로 실제 로드·`typeof XLSX`·콘솔 오류 0건을 확인했다.
- **`applyState`가 조작된 `#q=` 값을 걸러내지 않던 문제를 고쳤다.** 2026-09-09에
  "다음 정비 때 처리"로 남겨 둔 항목이다. 고치기 전 코드로 조작된 값(문자열 sel, 음수 qty)을
  넣어 재현하니 화면이 이상한 숫자를 보이는 정도가 아니라 `render()`가 `TypeError`로 그
  자리에서 죽는 것을 확인했다 — 예상보다 증상이 컸다. 사람이 입력칸에 직접 칠 때 쓰던
  검증 규칙(`Math.max(0,parseInt(...)||0)`, `fixPer()`)을 그대로 재사용해 고쳤고,
  `tests/scenarios.test.js` 시나리오 8로 고정했다(고치기 전 코드로 되돌려 실제
  `TypeError`가 나는 것까지 확인 후 되돌림). CSP 인라인 스크립트 해시도 함께 갱신했다.

## 2026-09-10

- **테스트 0개 상태를 벗어남.** 2026-09-09에 손으로 눌러 확인했던 7가지 상황(부호 양쪽·
  0원 경계값·집행률/게이지 색 경계·구분 번호·자동 계산 보정·`#q=` 주소 왕복)을 vitest로
  고정했다(`tests/scenarios.test.js`). index.html을 jsdom에 그대로 실행시켜 `render()`가
  실제로 그리는 화면(DOM)까지 검사한다 — 정규식으로 함수만 떼어 검사하던
  `scripts/check-*.js`와 달리 표시되는 숫자 자체를 본다.
- 9개 테스트 전부, 실제 코드를 하나씩 일부러 깨뜨려 빨간불이 뜨는 것을 확인한 뒤 되돌렸다
  (§5.4/§21-2). CI(`firebase-hosting.yml`)에 `npm test` 단계를 추가해 배포 전에 돈다.
- GitHub Actions 액션을 v4 → v7로 올려 "Node.js 20 is deprecated" 경고를 없앴다
  (`actions/checkout`, `actions/setup-node`; 검사용 node-version도 20 → 24).
- 되돌릴 지점으로 freeze 태그 `inky-calculator-freeze-20260910-pre-tests`를 찍었다.

## 2026-09-09

**Firebase Hosting으로 이전하고, 라이브에서 나온 결함 다수를 그날 안에 고쳤다.**

- 공개 주소를 GitHub Pages(`edutogether.github.io/inky-calculator`)에서
  **Firebase Hosting `calc.edutogether.kr`**로 옮겼다. CSP를 sha256 해시로(`'unsafe-inline'`
  없이) 걸고, 배포 워크플로가 CSP 해시·권장안 상한·부호를 검사한 뒤 배포하고 배포 후
  보안 헤더까지 확인하도록 만들었다(`.github/workflows/firebase-hosting.yml`,
  `scripts/check-*.js`).
- 첫 Firebase 배포에서 `.git` 디렉터리가 통째로 공개 배포되는 사고가 있었다(`**/.*` 패턴이
  하위 경로를 잡지 못함) — `firebase.json`의 ignore 목록을 고쳐 바로잡았다.
- **부호 결함**: 돈이 남는데 화면에 `-15,988원`으로 뜨던 것을 고쳤다. 계산은 맞았고 표시
  단계에서 `-`를 하드코딩해 둔 것이 원인이었다 — 남으면 `+`, 초과하면 `−`, 딱 맞으면
  부호 없음(`signed()`)으로 통일했다.
- **집행률 색 경계**: 화면은 소수 1자리로 반올림해 보여 주는데 판정은 원값으로 하고 있어
  같은 "100.0%"가 어떤 땐 초록·어떤 땐 빨강으로 보이던 것을 고쳤다. 판정을 화면 표시값
  기준으로 바꾸고, 정확히 100%는 초록(예산을 다 쓴 것은 최선)으로 확정했다.
- **게이지 막대 색**: 막대가 집행률과 다른 규칙(`pct>92`)을 써서 98.9%는 초록인데 막대는
  주황이던 것을, 막대도 `rateClass()`를 그대로 쓰도록 통일했다.
- **구분 번호**: 공통·부스·협의회비 여섯 구분에 ⓪①②③④⑤ 번호를 붙여 하나의 순서로
  보이게 했다.
- **탭 제목·파비콘**: 탭 제목을 `InKY Calculator`로, 파비콘을 🧾 이모지 SVG(`data:` URI)로
  추가했다. 탭이 비활성이면 흑백으로 바뀐다.
- **협의회비 개편**: 물품을 고를 때마다 협의회비 지급액이 뒤에서 자동으로 따라 계산되던
  것을 없앴다. 첫 진입에만 자동 계산을 한 번 돌려 권장안을 정하고, 그 뒤로는 사람이
  「자동 계산」을 눌러야만 값이 바뀐다.
- **인당 지원금액 상한**: 30,000원 → **40,000원**으로 정정하고(근거 문서 재확인),
  안내창에 근거 문서 링크 두 개(편성지침·집행지침)를 각각 무엇인지 설명과 함께 걸었다.
- **협의회비 총액 상한**: 배정 예산의 **40%**로 확정(문서 근거는 못 찾아 안내창에 법령
  링크는 붙이지 않음).
- **자동 계산 알고리즘**: 지급액 → 횟수 → 인원 순으로 맞추고, 흔한 조합(1회·10~20명)을
  먼저 탐색하며, 목표선(인당 10,000원) 이상 중 남는 돈이 가장 적은 조합을 고르도록 정리했다.
- **인쇄**: A4 세로 6쪽 기준으로 레이아웃을 맞추고, 마지막 쪽에 협의회비와 견적 합계가
  함께 나오도록 고쳤다. `.moutw`의 `flex:0 0 auto` 누락으로 좁은 화면·인쇄물에 빈 공간이
  생기던 것도 함께 고쳤다.
- `intent` 워크플로를 도입하고(`_docs/intents/`, `.claude/rules/intent-workflow.md`),
  이 저장소 전용 판단·사고 기록을 `.claude/rules/app.md`에 모았다.

## 2026-09-08 이전

- 초기 배포(`14262a3`) 이후 GitHub Pages 위에서 카카오톡 파일 미리보기 대응(`#nojs` 카드),
  공유 링크, 인쇄 안내창, 엑셀 저장 등을 순차적으로 붙여 나갔다. 이 구간의 세부 커밋은
  `git log`에서 그대로 확인할 수 있다 — 굵직한 흐름만 위 2026-09-09 항목에 이어진다.
